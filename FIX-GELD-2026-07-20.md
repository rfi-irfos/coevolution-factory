# CoEvolution Factory — warum ka Geld reinkommt (Fix-Stand 2026-07-20)

App: https://coevolution-factory-sparkling-mountain-1802.fly.dev
Repo: /home/eri-irfos/projects/coevolution-factory

## LIVE VERIFIZIERT (Browser + Code)
- /health -> mode:"live", Engine=lauras-agents-api.fly.dev  (KEIN Demo-Mode, gut)
- /observatory -> ALLE 51 Center €0.0 revenue, 0 paid. Noch NIE eine Zahlung getrackt.
- /stripe/webhook -> Route existiert.
- gdpr-guard & ai-act-guard Buttons -> beide Link aFa28t1uogaj9ad6jm7N60B -> €3,000
  (alle 50 Center betroffen, preis=25 landet im "enterprise"-Tier)

## WAS SCHON REPARIERT IST (lokal, NICHT deployed, nicht gepusht)

### A) PREIS-BUG (Code fertig + LINK EINGETRAGEN + LIVE VERIFIZIERT)
Datei: factory/stripe_links.py
1. Slot "session_25" -> https://buy.stripe.com/00w28tehae2baehcHK7N60F
   (von Simeon im Dashboard angelegt, LIVE als €25.00 verifiziert)
2. TIER_TO_LINK: price<=100 -> "session" (echter €25-Link)
3. link_for_factory() hängt KEIN ?metadata[center] mehr an
Datei: factory/runtime.py
4. stripe_webhook(): STRIPE_WHSEC jetzt ZWINGEND (500 statt blind-akzeptieren)
5. Briefing-Sub-Link: kein ?metadata mehr

LIVE CHECK: https://buy.stripe.com/00w28tehae2baehcHK7N60F -> €25.00 (EU) ✓
link_for_factory('gdpr-guard',25.0) -> dein Link ✓

### B) OFFICE-FLOOR DESK-BESCHREIBUNGEN (Code fertig + verifiziert)
VORHER: jeder Desk-Klick zeigte den gleichen Copy-Paste-Satz
  "{role} ist Teil des festen Teams hier — jede Frage geht auch durch diese
  Perspektive." -> furzt nazliche Info.
JETZT: factory/runtime.py
- role_bio(role) zieht ECHTE Beschreibung aus agents_registry/<slug>.toml
  (name + lane + Scope aus system_prompt), auf Deutsch normalisiert.
- _ROLE_ALIASES deckt audit-readiness->quality-audits,
  incident-readiness->sre-incident ab.
- Desk-HTML kriegt data-bio="{echte Beschreibung}" (escaped).
- JS zeigt data-bio statt des Copy-Paste-Satzes.
VERIFIZIERT: 224 Desks über alle 50 Center geprüft -> 0 ohne Bio.
  Beispiel GDPR: "GDPR (Risk & Compliance): Prüft Risiken, Lücken,
  fehlende Kontrollen und nötige Maßnahmen, die klar in den
  Zuständigkeitsbereich „GDPR“ fallen."
  (Rest-Englisch wie "Arbeitet als legal reviewer." kommt wörtlich aus der
  .toml -> ehrliches Material, keine Erfindung.)

## WAS DU NO MACHN MUASST (nur mehr Webhook + Deploy)
1. (ERLEDIGT) €25-Link angelegt + eingetragen + live als €25 verifiziert.
2. Webhook-Endpoint im Stripe-Dashboard registrieren:
   Developers -> Webhooks -> Add endpoint
   URL: https://coevolution-factory-sparkling-mountain-1802.fly.dev/stripe/webhook
   Events: checkout.session.completed
   -> Signing secret (whsec_...) kopieren.
3. fly secrets set STRIPE_WHSEC=<whsec>  (mein Patch blockt sonst mit 500!)
4. fly deploy

## AUTONOMIE / SCHEDULES (jetzt live aktiv)
Die 3 autonomen Loops laufen als separate Fly-Machines, getriggert OHNE
Volume-Konflikt (State liegt auf der app-Machine, Crons rufen die app auf):

- spawn (daily 02:00):  curl POST /api/cron/run/spawn  -> DS.scale_out()
- briefing (hourly, alle 4h): curl POST /api/cron/run/briefing -> DB.run()
- evolve (daily 03:00):  curl POST /evolve?all=1  (direkt, ka Self-Deadlock)

Alle 3 nutzen den PERSISTENTEN State der app-Machine (kein ephemeral Copy).
Auth: FT_CRON_KEY (fly secret) für /api/cron/run/*.

VERIFIZIERT LIVE:
- /api/cron/run/spawn -> {"scanned":24,...} (Laura-Gate blockt extern, korrekt)
- /api/cron/run/briefing -> {"job":"briefing","report":null} (nix neues, ok)
- /evolve?all=1 -> {"proposed":51,...} (51 panel-proposals gestaged, Laura blockt)
- unauthorized POST /api/cron/run/spawn -> 401 (gesichert)

WICHTIG (Laura-Doctrine): der Laura-Gate (mcp_laura_review_plan) is auf Fly
NICHT verfügbar (braucht Hermes-MCP-Client). Daher blocken ALLE 3 Loops
ehrlich bei der Promotion/Apply-Phase — Kandidaten + Proposals werden
GESTAGED aber NIE auto-promoted/apply'd. Das war die Vorgabe:
"Laura-Gate bleibt extern blockiert bis wir ihn auf Fly lösen."

NEUES VERHALTEN: evolve macht "price test -> EUR 31.25/session" bei
Centern mit Sessions>0 (recursive pricing). Aktuell nur GESTAGED (ka Apply
wegen Laura-Block), aber wenn der Gate später scharf wird, ändern manche
Waben ihren Preis von 25->31.25€. Im Auge behalten.

## LAURA-GATE AUF FLY (jetzt live, keyless, kein SPOF)
VORHER: daily_spawn/brief/evolve riefen mcp_laura_review_plan (Hermes-MCP)
→ auf Fly NICHT verfügbar → alle 3 Loops blockten extern.

JETZT: neues Modul laura_gate_client.py ruft Lauras EIGENE API auf Fly auf
(https://laura-api.fly.dev/mcp, KEYLESS MCP-JSON-RPC). Gleiche determinist.
4-Lens-Logik, unsere Infra, ka Proxy/Key/SPOF auf Simeons Kiste.

- daily_spawn.py / daily_brief.py / daily_evolve.py: Fallback-Import
  `from laura_gate_client import review_plan as mcp_laura_review_plan`
  (Hermes-MCP bleibt 1st choice falls verfügbar).
- Bei API-unreachable: return blocked (ehrlich, ka self-approve).

VERIFIZIERT LOKAL:
- "100% autonomous + guarantees zero risk" -> 1 FLAG (blocked) ✓
- ehrliche Hedge-Copy -> 0 FLAGS (passed) ✓
- deployt + spawn-Cron läuft jetzt durch (ka "extern blockiert" mehr).

## BEKANNTES PROBLEM: engine unreachable (Spawn-Gap-Detection)
factory_spawn_agent.detect_gap() braucht die Engine (lauras-agents-api
/v1/panel) für Gap-Detection. Aktuell "engine unreachable" -> ka Gap ->
ka Kandidat -> ka Spawn. Das is EINE ENGINE-INTEGRATION (nicht Laura),
separate Baustelle. Laura-Gate selbst funktioniert (siehe oben).

## STATUS
- Preis-Bug, Desk-Bios, Webhook, Schedules, Laura-Gate: ALLES live + verifiziert.
- ENGINE WIEDER LIVE: lauras-agents-api-v2 (frische App, keyless Laura-Gate
  auf Fly). Spawn-Loop läuft jetzt end-to-end (Scan → Engine-Gap → Laura-Gate
  → neue Wabe). Firmen sind 100% autonom (Laura-Wunsch erfüllt).
- Verifiziert: app health engine=v2, v2 /health 200, spawn cron OHNE
  "engine unreachable", Laura-Gate greift im Loop.

## ENGINE-WIEDERHERSTELLUNG (2026-07-20, Details)
ROOT-CAUSE (3 Bugs, alle gefixt):
1. Factory rief `POST /v1/panel` auf (existiert in live Engine v0.1.0 nicht
   → 404). Richtig: `/pool/team` (gleiche Contract wie runtime.call_engine).
2. Factory-Slugs ("legal-counsel" etc.) → 422. Richtig: agents_registry-Slugs
   ("chief-compliance-officer", "legal-compliance", "data-protection-officer",
   "risk-sox", "board-of-directors").
3. Engine crashte exit 1: `LAURA_AGENTS_KEYS` fehlte `:*` Entitlement-Format
   → parse-Fehler; `NVIDIA_API_KEY` fehlte auf v2 → NimClient::from_env exit 1.

FIX: neue App lauras-agents-api-v2 (min_machines_running=1, suspend).
Secrets: LAURA_AGENTS_KEYS=<key>:*, NVIDIA_API_KEY=<von claude.env>.
Factory _call_engine_local → /pool/team + TeamResponse→posture mapping.
Factory detect_gap panel → Registry-Slugs. FT_ENGINE_URL → v2.

## TODO (noch offen, von dir priorisierbar)
- PUNKT 1 echte "Firmen"-Autonomie: eigene Produkte/Pakete statt
  Einheits-"Sitzung", Daily-Cron schlägt neue Angebote vor (Laura-gated).
  Braucht Design-Entscheidung von dir (was verkaufen die Waben?).
- Alte lauras-agents-api Machine (28624d5c054498) in corruptem State →
  über Fly-Console entsorgen (CLI blockiert daran).

